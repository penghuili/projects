import { SecondFormatterPipe } from './second-formatter.pipe';

describe('SecondFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
